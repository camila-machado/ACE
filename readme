##------------------------------------------------------------------------------
# CNPEM - Centro Nacional de Pesquisa em Energia e Materiais, Brazil
# Sirius - Research Group EMA
#
# Code project: TC_caculus
# 
# Objectif:
# Automate the use of Quantum Espresso (QE) for the calculus of critical 
# superconductivity temperature of diferent molecules and cell structures
# 
##------------------------------------------------------------------------------

#Instructions in how to use TC_calculus

#->Unpack .tar.gz
#->Open terminal in unpacked folder 
#->Open write_config.py and change variables at will
#->Remember to save the pseudo potential files you need in 'pseudo' folder
#->Add Quantum Espresso and Program.py to Linux path(*)
#->Enable Program.py as executable(**)

#call in terminal:

program.py directory/file_name.cif

#Enjoy your TC's!!!

(*) You can run the commands in shell Linux environment:
$ export PATH = $PATH:<quantum_espresso_bin_dir>
$ export PATH = $PATH:<Program.py_dir>
$ export PATH = $PATH:<Write.py_dir>
$ echo $PATH (to check result)
obs: temporary solution, valid only in current shell

or

add the command lines to the .bashrc file for permanet solution, and run:
$ souce .bashrc (to execut alterations and make them valid)

(**) Run the command:
$ chmod +x program.py
$ chmod +x write.py

#-------------------------------------------------------------------------------
#New features v-2.0:

#-> Automatically find pseudopotential files from pseudo folder
#-> Replace os.system  with new subprocess module
#-> Make program.py an executable file
#-> Add exception treatment
#-> Improve organization of output files
#-> Save database with prefix in file name
#-> Save cell structure input file 
#-> Count execution time
